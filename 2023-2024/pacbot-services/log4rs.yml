appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d(%Y-%m-%d %H:%M:%S)} {h({l})} [{t}] {m}{n}"

root:
  level: trace
  appenders:
    - stdout

loggers:
  tokio_tungstenite:
    level: info
  tungstenite:
    level: info